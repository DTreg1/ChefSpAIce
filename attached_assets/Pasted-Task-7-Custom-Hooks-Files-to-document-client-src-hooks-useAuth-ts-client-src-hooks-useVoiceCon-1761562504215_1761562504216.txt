Task 7: Custom Hooks
Files to document:
* client/src/hooks/useAuth.ts
* client/src/hooks/useVoiceConversation.ts
* client/src/hooks/useBarcodescanner.ts
* client/src/hooks/useProgressiveDisclosure.ts
Documentation Focus:
/**
* useAuth Hook
*
* Manages authentication state and user session.
* Integrates with Replit Auth OIDC provider.
*
* Returns:
* - user: Current user object or null
* - isLoading: True during initial auth check
* - login: Function to initiate OIDC login flow
* - logout: Function to clear session
*
* State Persistence:
* - [describe if using localStorage, session, or cookies]
*
* Auth Flow:
* 1. Component mounts → check existing session
* 2. User clicks login → redirect to Replit Auth
* 3. Callback returns → exchange code for token
* 4. Store token → fetch user profile
* 5. Update state → re-render protected UI
*
* Usage:
* const { user, login, logout } = useAuth();
* if (!user) return <LoginButton onClick={login} />;
*/

/**
* useVoiceConversation Hook
*
* Real-time voice interaction with AI recipe assistant.
* Uses Web Speech API for voice input + OpenAI for responses.
*
* Features:
* - Speech Recognition: Browser's native speech-to-text
* - AI Conversation: Streaming responses from OpenAI
* - Audio Playback: Text-to-speech for AI responses
* - Conversation History: Maintains context for multi-turn dialog
*
* Returns:
* - isListening: Microphone active state
* - transcript: Real-time speech-to-text
* - response: AI's text response
* - startListening: Begin voice input
* - stopListening: End voice input
*
* Technical Details:
* - Browser API: webkitSpeechRecognition or SpeechRecognition
* - Language: [specify default language]
* - Continuous: [true/false for continuous recognition]
* - Interim results: [true/false for real-time transcription]
*/

/**
* useBarcodeScanner Hook
*
* Barcode scanning using device camera + html5-qrcode library.
* Integrates with barcode lookup API to fetch product data.
*
* Returns:
* - isScanning: Camera active state
* - scannedCode: Detected barcode number
* - product: Product data from lookup API
* - error: Scanner or API errors
* - startScanning: Activate camera
* - stopScanning: Deactivate camera
*
* Flow:
* 1. User clicks "Scan Barcode"
* 2. Request camera permission
* 3. Initialize html5-qrcode scanner
* 4. Detect barcode → extract number
* 5. Call /api/barcode/lookup/{code}
* 6. Return product data or null if not found
*
* Camera Selection: [describe front/back camera logic]
* Supported Formats: UPC, EAN, Code128, etc.
* Error Handling: Permission denied, no camera, API failure
*/
