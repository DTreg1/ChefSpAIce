Enhance the appliances API to include category information with each appliance
and enable filtering/grouping by category.

BACKEND CHANGES:
1. Update appliances API response (server/routers/*.ts):
- Include category field in appliance serialization
- Ensure category is populated from database
- Add category to JSON response for each appliance

2. Update storage methods:
- Modify getAllAppliances() to include category join/select
- Update getApplianceById() to return category
- Ensure category is part of the appliance type definition

3. Add category-specific endpoints:
- GET /api/appliances/categories - list all categories with counts
- GET /api/appliances?category=:categoryId - filter by category
- Response format:
{
id: "uuid",
name: "Air Fryer",
category: {
id: "uuid",
name: "Cooking Appliances",
icon: "ChefHat"
},
...other fields
}

DATABASE VERIFICATION:
1. Check applianceCategories table exists
2. Verify foreign key relationship: appliances.categoryId -> applianceCategories.id
3. Ensure sample categories are seeded:
- Cooking Appliances (ovens, stoves, air fryers)
- Food Preparation (blenders, food processors)
- Storage (refrigerators, freezers)
- Cleaning (dishwashers)
- Specialty (sous vide, pressure cookers)

FRONTEND UPDATES:
1. Remove TODO comment from appliances.tsx:168
2. Update TypeScript interfaces:
interface Appliance {
id: string;
name: string;
category: {
id: string;
name: string;
icon?: string;
};
// ...other fields
}

3. Update category filter dropdown:
- Fetch categories from API
- Show category names in dropdown
- Filter appliances by selected category
- Show appliance count per category

4. Group appliances by category in display:
- Add "Group by Category" toggle
- Collapsible category sections
- Category headers with icons
- Count of appliances in each category

5. Update queries to use category data:
const { data: appliances } = useQuery({
queryKey: ['/api/appliances'],
});

const { data: categories } = useQuery({
queryKey: ['/api/appliances/categories'],
});

UI ENHANCEMENTS:
1. Category badge on each appliance card
2. Category icon next to appliance name
3. Filter chips for active category filter
4. Category-based color coding (optional)

TESTING:
- Verify category data appears in API responses
- Test category filtering works correctly
- Check grouping displays properly
- Validate category dropdown populates
- Test with appliances that have no category (edge case)
