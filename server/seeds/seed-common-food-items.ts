/**
 * Seed Common Food Items
 *
 * This file was used for seeding onboarding inventory items.
 * The functionality has been moved to the main application flow.
 */

/**
 * Seeds the common food items.
 * Currently a no-op as the data seeding has been integrated elsewhere.
 */
export async function seedCommonFoodItems(_forceUpdate = false) {
  // Functionality has been integrated into the application
  // This function is kept for backwards compatibility
  return {
    successCount: 0,
    errorCount: 0,
    elapsedTime: 0,
    errors: [],
  };
}
