# Environment Configuration for Docker Development
# Copy this file to .env and fill in your values
# ================================================

# Application Environment
NODE_ENV=development
PORT=5000

# Database Configuration
# For Docker: use 'postgres' as host (service name in docker-compose)
# For local development without Docker: use 'localhost'
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=myapp
DATABASE_URL=postgresql://postgres:postgres@postgres:5432/myapp

# Session Configuration
SESSION_SECRET=your-super-secret-session-key-change-this-in-production

# API Configuration
VITE_API_URL=http://localhost:5000

# ================================================
# Optional Integrations (uncomment and configure as needed)
# ================================================

# Stripe Payment Integration
# Get these from https://dashboard.stripe.com/test/apikeys
# STRIPE_SECRET_KEY=sk_test_...
# VITE_STRIPE_PUBLIC_KEY=pk_test_...

# OpenAI Integration
# Get your API key from https://platform.openai.com/api-keys
# OPENAI_API_KEY=sk-...

# USDA FoodData Central API (if using nutrition features)
# Get your API key from https://fdc.nal.usda.gov/api-guide.html
# USDA_FDC_API_KEY=your_usda_api_key_here

# Replit Auth / OIDC Configuration
# Configure these if using Replit authentication
# ISSUER_URL=https://replit.com
# CLIENT_ID=your-client-id
# CLIENT_SECRET=your-client-secret
# VITE_REPLIT_AUTH_ENABLED=false

# Object Storage (for file uploads)
# PUBLIC_OBJECT_SEARCH_PATHS=/public/uploads
# PRIVATE_OBJECT_DIR=/private/uploads

# Firebase Push Notifications
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=your-client-email
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----"

# Email Configuration (SMTP)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password
# EMAIL_FROM=noreply@yourdomain.com

# ================================================
# Cache Configuration (for API responses)
# ================================================
CACHE_ENABLED=true
CACHE_MAX_SIZE=10000
CACHE_DEFAULT_TTL_DAYS=30
CACHE_DEBUG=false

# Cache TTL for different data types (in days)
CACHE_TTL_FOOD_DAYS=30
CACHE_TTL_SEARCH_DAYS=7
CACHE_TTL_NUTRIENTS_DAYS=90
CACHE_TTL_BRANDED_DAYS=14
CACHE_TTL_BARCODE_DAYS=14

# ================================================
# Admin Configuration
# ================================================
ADMIN_EMAILS=admin@example.com

# ================================================
# pgAdmin Configuration (Database Management UI)
# Access at http://localhost:5050
# ================================================
PGADMIN_EMAIL=admin@admin.com
PGADMIN_PASSWORD=admin

# ================================================
# Development Settings
# ================================================
VITE_DEV_MODE=true
VITE_SHOW_DEBUG=true

# ================================================
# Production Settings (when NODE_ENV=production)
# ================================================
# DOMAIN=yourdomain.com
# HTTPS_ENABLED=true
# RATE_LIMIT_ENABLED=true
# RATE_LIMIT_MAX_REQUESTS=100
# RATE_LIMIT_WINDOW_MS=900000

# ================================================
# Docker-specific Settings
# ================================================
# These are used by docker-compose.yml
# Usually, you don't need to change these
COMPOSE_PROJECT_NAME=fullstack-app
DOCKER_BUILDKIT=1